class Team {
  final String abbreviation;
  final String city;
  final String fullName; // Agregamos el campo full_name

  Team({
    required this.abbreviation,
    required this.city,
    required this.fullName,
  });
}
